import HomeView from 'src/sections/build/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Wentworth: Home',
};

export default function Page() {
  return <HomeView />;
}
